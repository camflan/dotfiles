# Dotfiles

- `bin/` — standalone scripts, added to $PATH
- `config/` — app configs, symlinked by install.conf.yaml
- `config/claude/` — Claude Code config (symlinked to ~/.claude/)
- Managed by dotbot (install.conf.yaml)
